// Intentionally left as a placeholder.
// v0.0.3 dropped the WPF-based IconFactory in favour of
// System.Windows.Forms.NotifyIcon, which extracts the .ico embedded
// in the .exe itself via Icon.ExtractAssociatedIcon. See TrayHost.cs.
//
// Kept as an empty file so existing references / git history stay clean.
namespace FreeScreenshot;
internal static class IconFactory { }
