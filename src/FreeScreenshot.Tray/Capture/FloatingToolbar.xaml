<Window x:Class="FreeScreenshot.Capture.FloatingToolbar"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FreeScreenshot"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        SizeToContent="WidthAndHeight"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        KeyDown="OnKeyDown">
  <Border Background="#F225221E"
          BorderBrush="#FF524C44" BorderThickness="1"
          CornerRadius="22" Padding="6"
          Effect="{x:Null}">
    <StackPanel Orientation="Horizontal" x:Name="ButtonsHost"/>
  </Border>
</Window>
