<Window x:Class="Freezshot.Capture.CapturePreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Freezshot — preview"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="Manual"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        MouseEnter="OnMouseEnter"
        MouseLeave="OnMouseLeave"
        MouseLeftButtonDown="OnClick"
        Opacity="0">
  <Window.Triggers>
    <EventTrigger RoutedEvent="Window.Loaded">
      <BeginStoryboard>
        <Storyboard>
          <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:0.2"/>
        </Storyboard>
      </BeginStoryboard>
    </EventTrigger>
  </Window.Triggers>

  <Grid>
    <Border x:Name="Frame"
            CornerRadius="14" Background="#FF112528"
            BorderBrush="#FF1F3A3D" BorderThickness="1"
            Padding="6">
      <Border.Effect>
        <DropShadowEffect Color="#FF000000" Opacity="0.55" BlurRadius="22" ShadowDepth="0"/>
      </Border.Effect>
      <StackPanel>
        <Border CornerRadius="10" ClipToBounds="True">
          <Image x:Name="ShotImage" Stretch="None" Cursor="Hand"/>
        </Border>
        <TextBlock x:Name="Caption" Margin="4,6,4,2" FontSize="11"
                   Foreground="#FFA8BDB8" TextTrimming="CharacterEllipsis"/>
      </StackPanel>
    </Border>
    <Button x:Name="CloseBtn"
            Width="22" Height="22" Margin="0,-8,-8,0"
            HorizontalAlignment="Right" VerticalAlignment="Top"
            Background="#FF193033" Foreground="#FFF0FAF7"
            BorderBrush="#FF345456" BorderThickness="1"
            FontSize="13" FontWeight="Bold" Padding="0"
            Cursor="Hand"
            FocusVisualStyle="{x:Null}"
            Content="×"
            Click="OnCloseClick"/>
  </Grid>
</Window>
