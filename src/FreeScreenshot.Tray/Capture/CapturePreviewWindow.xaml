<Window x:Class="FreeScreenshot.Capture.CapturePreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FreeScreenshot — preview"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="Manual"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        MouseEnter="OnMouseEnter"
        MouseLeave="OnMouseLeave"
        MouseLeftButtonDown="OnClick"
        Opacity="0">
  <Window.Triggers>
    <EventTrigger RoutedEvent="Window.Loaded">
      <BeginStoryboard>
        <Storyboard>
          <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:0.2"/>
        </Storyboard>
      </BeginStoryboard>
    </EventTrigger>
  </Window.Triggers>

  <Grid>
    <Border x:Name="Frame"
            CornerRadius="10" Background="#FF25221E"
            BorderBrush="#FF524C44" BorderThickness="1"
            Padding="6">
      <Border.Effect>
        <DropShadowEffect Color="#FF000000" Opacity="0.55" BlurRadius="22" ShadowDepth="0"/>
      </Border.Effect>
      <StackPanel>
        <Border CornerRadius="6" ClipToBounds="True">
          <Image x:Name="ShotImage" Stretch="None" Cursor="Hand"/>
        </Border>
        <TextBlock x:Name="Caption" Margin="2,6,2,2" FontSize="11"
                   Foreground="#FFA8A39B" TextTrimming="CharacterEllipsis"/>
      </StackPanel>
    </Border>
    <Button x:Name="CloseBtn"
            Width="22" Height="22" Margin="0,-8,-8,0"
            HorizontalAlignment="Right" VerticalAlignment="Top"
            Background="#FF25221E" Foreground="#FFF5F2EC"
            BorderBrush="#FF524C44" BorderThickness="1"
            FontSize="13" FontWeight="Bold" Padding="0"
            Cursor="Hand"
            Content="×"
            Click="OnCloseClick"/>
  </Grid>
</Window>
